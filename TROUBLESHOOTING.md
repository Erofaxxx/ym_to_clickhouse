# Решение проблемы 400 Bad Request

## Проблема

Пользователь получал ошибку при запуске `load_ym_to_clickhouse.py`:

```
ERROR - Error during data load: API returned status 400: Неизвестное поле в запросе: ym:s:DirectPlatform для source visits
```

или

```
ERROR - Error during data load: Failed to check Logs API availability: 400 Client Error: Bad Request
```

## Причины ошибки 400 Bad Request

Ошибка 400 Bad Request от Yandex Metrica API может возникать по следующим причинам:

1. **Недоступные поля (основная причина)** - Некоторые поля недоступны для определенных типов счетчиков:
   - `ym:s:DirectPlatform` и `ym:s:DirectConditionType` - поля Яндекс.Директ, доступны только для счетчиков с интеграцией Директа
   - Поля электронной коммерции - доступны только если настроена электронная коммерция
   - Поля рекламных систем - доступны только при наличии рекламных интеграций

2. **Слишком большой период** - Запрос данных за большой период времени

3. **Неверный формат параметров** - Ошибки в формате дат или других параметров

4. **Превышены лимиты API** - Слишком много одновременных запросов

## Реализованные улучшения

### 1. Автоматическое определение доступных полей (НОВОЕ!)

**Главное исправление:** Программа теперь **автоматически определяет**, какие поля доступны для вашего счетчика, и использует только их. Не требуется ручной подбор полей!

**Как это работает:**
1. При запуске программа тестирует все 38 полей через API
2. Если какие-то поля недоступны (например, `DirectPlatform`, `TrafficSource` или поля электронной коммерции), программа автоматически их исключает
3. Программа использует только те поля, которые действительно доступны для вашего счетчика
4. ClickHouse таблица создается динамически под доступные поля

**Преимущества:**
- ✅ Работает для любого счетчика без модификаций
- ✅ Не требуется ручное редактирование списка полей
- ✅ Автоматически адаптируется под особенности вашего счетчика

Теперь программа начинает с **38 полей** и автоматически убирает недоступные:
- Базовый список: все стандартные поля визитов
- Автоматически исключаются: поля Яндекс.Директ, неактивные поля электронной коммерции, недоступные рекламные поля

### 2. Улучшенная обработка ошибок

**До:**
```python
response.raise_for_status()  # Просто выбрасывает исключение
```

**После:**
```python
if response.status_code != 200:
    error_msg = f"API returned status {response.status_code}"
    try:
        error_data = response.json()
        if 'message' in error_data:
            error_msg += f": {error_data['message']}"
        if 'errors' in error_data:
            error_msg += f". Errors: {error_data['errors']}"
    except:
        error_msg += f". Response: {response.text[:200]}"
    raise YandexMetricaAPIError(error_msg)
```

Теперь программа показывает:
- Конкретный код ошибки
- Сообщение об ошибке от API (например, "Неизвестное поле в запросе: ym:s:DirectPlatform")
- Детальную информацию о проблеме

### 3. Валидация дат

Добавлена проверка дат перед отправкой запроса:

```python
# Validate dates
start_date = datetime.strptime(self.config['start_date'], '%Y-%m-%d')
end_date = datetime.strptime(self.config['end_date'], '%Y-%m-%d')
today = datetime.now()

if start_date > today or end_date > today:
    raise ValueError(f"Dates cannot be in the future. Today is {today.strftime('%Y-%m-%d')}")

if start_date > end_date:
    raise ValueError(f"Start date cannot be after end date")
```

### 4. Скрипт диагностики troubleshoot.py

Создан новый скрипт для диагностики проблем **ДО** запуска основной программы:

```bash
python troubleshoot.py --config config.json
```

Скрипт проверяет:

1. **Формат токена** - проверяет наличие и длину токена
2. **Доступ к счетчику** - пытается получить информацию о счетчике
3. **Корректность дат** - проверяет формат и диапазон дат
4. **Доступность минимальных полей** - тестирует с 3 базовыми полями
5. **Доступность всех 40 полей** - проверяет, доступны ли все запрашиваемые поля

#### Пример вывода при успехе:

```
=== Testing Yandex Metrica API Access ===

1. Checking token format...
   ✓ Token present (length: 39)

2. Testing counter access...
   ✓ Counter found: My Website

3. Checking dates...
   Start date: 2026-01-10
   End date: 2026-02-10
   Today: 2026-02-15
   ✓ Date range: 31 days

4. Testing Logs API with minimal fields...
   ✓ Logs API is accessible
   Expected data size: 1234567 bytes

5. Testing Logs API with all 40 fields...
   ✓ All fields are available!
   Expected data size: 12345678 bytes

==================================================
✓ All tests passed! You should be able to load data.
==================================================
```

#### Пример вывода при ошибке:

```
5. Testing Logs API with all 40 fields...
   ❌ Error: API returned status 400
   Message: Some fields are not available
   Errors: {'fields': ['ym:s:productsPurchaseID', 'ym:s:productsID']}

   💡 Suggestion: Some e-commerce or advertising fields might not be
      available if those features are not used in your counter.
      Consider removing fields you don't need.
```

## Инструкция по использованию

### Шаг 1: Просто запустите программу!

```bash
python load_ym_to_clickhouse.py --config config.json
```

**Больше ничего не нужно делать!** Программа автоматически:
- Определит доступные поля для вашего счетчика
- Исключит недоступные поля
- Создаст таблицу в ClickHouse с правильной схемой
- Загрузит данные

### Шаг 2 (опционально): Диагностика при проблемах

Если возникают проблемы, используйте скрипт диагностики:

```bash
python troubleshoot.py --config config.json
```

Это поможет выявить проблемы с токеном, доступом к счетчику или датами.

### Что делать при ошибках

**Ошибка доступа (403):**
- Проверьте правильность токена в config.json
- Убедитесь, что у токена есть права на чтение данных

**Ошибка счетчика (404):**
- Проверьте правильность counter_id в config.json

**Ошибка дат:**
- Убедитесь, что даты в формате YYYY-MM-DD
- Даты не должны быть в будущем

**Все остальные ошибки 400:**
- Программа автоматически обработает недоступные поля
- Если проблема сохраняется, запустите troubleshoot.py для диагностики

## Дополнительные рекомендации

1. **Начните с малого периода** - Первый раз запускайте с периодом в 1-2 недели, чтобы убедиться, что все работает

2. **Проверьте настройки счетчика** - Убедитесь, что в настройках Яндекс.Метрики включены нужные функции (электронная коммерция, цели и т.д.)

3. **Используйте меньше полей** - Если вам не нужны все 40 полей, удалите ненужные для ускорения загрузки и уменьшения вероятности ошибок

4. **Проверьте лимиты** - У Logs API есть лимиты на размер выгрузки и количество запросов. Проверьте документацию: https://yandex.ru/dev/metrika/doc/api2/logs/intro.html

## Полезные ссылки

- [Документация Logs API](https://yandex.ru/dev/metrika/doc/api2/logs/intro.html)
- [Список доступных полей для визитов](https://yandex.ru/dev/metrika/doc/api2/logs/fields/visits.html)
- [Получение OAuth токена](https://yandex.ru/dev/oauth/doc/dg/tasks/get-oauth-token.html)
